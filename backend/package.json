{
  "name": "human-recognition-and-alert-model",
  "version": "1.0.0",
  "description": "A human in disater recognition system built with IBM AI libraries",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "lambda-local -f server -e event.json -t 40"
  },
  "author": "Sami Ahmad Khan",
  "license": "ISC",
  "dependencies": {
    "aws-serverless-express": "^3.3.6",
    "body-parse": "^0.1.0",
    "express": "^4.17.0",
    "ibm-watson": "^4.1.1",
    "watson-developer-cloud": "^4.0.1"
  },
  "devDependencies": {
    "aws-lambda-local": "^2.0.6"
  }
}
